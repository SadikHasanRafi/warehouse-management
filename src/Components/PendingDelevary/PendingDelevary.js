import React from 'react';

const PendingDelevary = () => {
    return (
        <div>
            <h1>If the product mark as pending delevary then it will be add here</h1>
            <button>See all pending delevary list</button>
        </div>
    );
};

export default PendingDelevary;