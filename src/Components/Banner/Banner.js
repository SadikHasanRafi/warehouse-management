import React from 'react';

const Banner = () => {
    return (
        <div>
            <h1>This is banner or slider depends on time</h1>
            <button type="submit">A button to open a specific card or to go manage items</button>
        </div>
    );
};

export default Banner;