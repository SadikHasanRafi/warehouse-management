import React from 'react';

const PendingOrder = () => {
    return (
        <div>
            <h1>If the ordered product mark as pending Order then it will be add here</h1>
            <button>See all pending order list</button>
        </div>
    );
};

export default PendingOrder;